{% extends 'base.html.twig' %}

{% block title %}Lesson index{% endblock %}

{% block body %}

    <h3>Предметы</h3>

    {% for lesson in lessons %}
        {{ lesson.getNumberLesson }}.
        <a href="{{ path('lesson_show', {'id': lesson.getId}) }}">{{ lesson.NameLesson }}</a>
        </br>
        </br>
    {% endfor %}

{% endblock %}
