{% extends 'base.html.twig' %}

{% block title %}Lesson index{% endblock %}

{% block body %}

    <h3>Предметы</h3>

    {% for lesson in lessons %}
        {{ lesson.getNumber }}.
        <a href="{{ path('lesson_show', {'id': lesson.getId}) }}">{{ lesson.Name}}</a>
        </br>
        </br>
    {% endfor %}

{% endblock %}
