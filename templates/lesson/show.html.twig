{% extends 'base.html.twig' %}

{% block title %}Lesson{% endblock %}

{% block body %}
    <h1>{{ lesson.nameLesson }}</h1>

    <a href="{{ path('course_show', {'id': lesson.course.id}) }}">
        Курс:{{ lesson.course.getName}}
    </a>

    </br> </br>
    <a href="{{ path('lesson_edit', {'id': lesson.id}) }}">Редактировать</a>

    {{ include('lesson/_delete_form.html.twig') }}
    </br>
    Описание: {{ lesson.content }}
{% endblock %}
