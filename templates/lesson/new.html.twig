{% extends 'base.html.twig' %}

{% block title %}New Lesson{% endblock %}

{% block body %}
    <h1>Новый урок</h1>
    <h3><a href="{{ path('course_show', {'id': lesson.course.id}) }}">
            {{ lesson.course.getNameCourse }}
        </a>
    </h3>
    {{ include('lesson/_form.html.twig') }}

{% endblock %}